package de.motine.wetterbild;

public class Custom {
  public static final String OPEN_WEATHER_KEY = "CHANGE_ME!!!!";
  public static final String OPEN_WEATHER_CITY = "Wirges,de"; // check valid city names here: http://openweathermap.org/current#name
  public static final String PHOTO_FOLDER = "/storage/sdcard1/wetterbild"; // this is evil!
  // We should be expanding the path with something like File(Environment.getExternalStorageDirectory(), "MYPATH").
  // But somehow the Fire only returns the "/storage/emulated/0" storage.
  // When I add a file picker at one stage, this problem will solve itself.
}